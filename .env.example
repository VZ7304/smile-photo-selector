# Frontend
VITE_API_BASE_URL=http://localhost:8787

# Worker non-secret config is kept in wrangler.toml vars.
# Secrets must be added with `wrangler secret put ...` or local `.dev.vars`.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=
