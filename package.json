{
  "name": "smile-photo-selector",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "npm run dev -w @smile/web",
    "dev:worker": "wrangler dev --config apps/worker/wrangler.toml",
    "build": "npm run build -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "npm run test -ws --if-present",
    "check": "npm run typecheck && npm run test",
    "db:migrate:local": "wrangler d1 migrations apply smile-photo-selector-dev --local --config apps/worker/wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply smile-photo-selector-dev --remote --config apps/worker/wrangler.toml"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.119.0"
  },
  "engines": {
    "node": ">=22"
  }
}
