name = "smile-photo-selector-api"
main = "apps/worker/src/index.ts"
compatibility_date = "2026-08-19"
workers_dev = true

[build]
command = "npm run build -w @smile/web"

[assets]
directory = "./apps/web/dist"
not_found_handling = "single-page-application"
run_worker_first = ["/api/*"]

[vars]
APP_ENV = "production"
APP_VERSION = "0.3.0"
CORS_ORIGIN = "http://localhost:5173"

# GOOGLE_OAUTH_CLIENT_ID is configured in Cloudflare as a secret binding.
# The browser receives it only through authenticated GET /api/v1/admin/drive/config.

[[d1_databases]]
binding = "DB"
database_name = "smile-photo-selector-dev"
database_id = "e08d4779-8841-4fc3-a80b-58706bed942a"
migrations_dir = "migrations"
